.row
  .col-lg-12
    .card
      .card-header
        = gettext "New group"
      .card-block
        = render "form.html", changeset: @changeset,
                              action: admin_group_path(@conn, :create),
                              conn: @conn
